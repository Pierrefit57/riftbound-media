---
import { getCollection } from 'astro:content';
import MainLayout from '../layouts/MainLayout.astro';

const allRules = await getCollection('rules');
const sortedRules = allRules.sort((a, b) => a.data.order - b.data.order);

const renderedRules = await Promise.all(
  sortedRules.map(async (rule) => {
    const { Content } = await rule.render();
    return { data: rule.data, Content };
  })
);
---

<MainLayout title="Règles du Jeu">

  <div class="max-w-4xl mx-auto px-4 sm:px-6 pt-10 pb-16">

    <!-- Page Header -->
    <div class="mb-10">
      <h1 class="text-3xl md:text-4xl font-display font-bold text-rift-100 mb-2">Règles du Jeu</h1>
      <p class="text-rift-300">Tout ce que vous devez savoir pour maîtriser Riftbound</p>
    </div>

    <!-- Table of Contents -->
    <nav class="mb-10 p-5 rounded-xl bg-rift-800/50 border border-rift-700/50">
      <h2 class="text-sm font-display font-semibold uppercase tracking-wider text-accent-forge mb-3">Sommaire</h2>
      <ul class="space-y-2">
        {sortedRules.map((rule, i) => (
          <li>
            <a href={`#section-${i}`} class="flex items-center gap-3 text-sm text-rift-300 hover:text-accent-spirit transition-colors group">
              <span class="text-lg">{rule.data.icon}</span>
              <span class="group-hover:translate-x-0.5 transition-transform">{rule.data.title}</span>
            </a>
          </li>
        ))}
      </ul>
    </nav>

    <!-- Rules Sections -->
    <div class="space-y-12">
      {renderedRules.map(({ data, Content }, i) => (
        <section id={`section-${i}`} class="scroll-mt-24">
          <div class="flex items-center gap-3 mb-6 pb-3 border-b border-rift-700/50">
            <span class="text-2xl">{data.icon}</span>
            <h2 class="text-xl font-display font-bold text-rift-100">{data.title}</h2>
          </div>
          <div class="prose prose-invert prose-orange max-w-none
            prose-headings:font-display prose-headings:font-bold prose-headings:text-rift-100
            prose-h2:text-lg prose-h2:mt-6 prose-h2:mb-3
            prose-h3:text-base prose-h3:mt-5 prose-h3:mb-2
            prose-p:text-rift-300 prose-p:leading-relaxed
            prose-a:text-accent-spirit prose-a:no-underline hover:prose-a:underline
            prose-strong:text-rift-100
            prose-li:text-rift-300
            prose-blockquote:border-accent-spirit/50 prose-blockquote:bg-accent-spirit/5 prose-blockquote:rounded-r-lg prose-blockquote:py-1 prose-blockquote:px-4
            prose-table:text-sm
            prose-th:text-accent-spirit prose-th:font-semibold
            prose-td:text-rift-300
            prose-hr:border-rift-700/50">
            <Content />
          </div>
        </section>
      ))}
    </div>
  </div>

</MainLayout>
